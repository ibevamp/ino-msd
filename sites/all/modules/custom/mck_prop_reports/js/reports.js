Drupal.behaviors.mck_prop_report = {
    attach: function (context, settings) {

            // Apply the myCustomBehaviour effect to the elements only once.
        $('.tooltip').tooltipster();
    }
};